@model List<ResultCarListWithBrandAdminDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/UIAdmin/Index.cshtml";
}

<style>
    .small-text {
        font-size: 0.9rem;
    }

    .small-header th {
        font-size: 0.95rem;
    }

    .small-btn {
        font-size: 0.85rem;
        padding: 0.25rem 0.5rem;
    }
</style>

<div>
    <div class="card">
        <div class="card-body">
            <h4 class="card-title small-text">Araba Verileri</h4>

            <div class="table-responsive">
                <table class="table table-striped mb-0 small-text">

                    <thead class="small-header">
                        <tr>
                            <th>#</th>
                            <th>Model</th>
                            <th>Marka</th>
                            <th>Fiyat</th>
                            <th>Güncelle</th>
                            <th>Sil</th>
                            <th>Özellikler</th>
                            <th>Lokasyonlar</th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <th scope="row">@item.carID</th>
                                <td>@item.model</td>
                                <td>@item.name</td>
                                <td>@item.carPricing</td>
                                <td><a href="/AdminCar/UpdateCar/@item.carID" class="btn btn-success small-btn">Güncelle</a></td>
                                <td><a href="/AdminCar/DeleteCar/@item.carID" class="btn btn-danger small-btn">Sil</a></td>
                                <td><a href="/Admin/CarFeature/Index/@item.carID" class="btn btn-primary small-btn">Git</a></td>
                                <td><a href="/Admin/CarLocations/Index/@item.carID" class="btn btn-primary small-btn">Git</a></td>
                            </tr>
                        }

                    </tbody>
                </table>
                <div class="mt-2">
                    <a href="/AdminCar/CreateCar/" class="btn btn-primary small-btn">Ekle</a>
                </div>
            </div>
        </div>
    </div>
</div>