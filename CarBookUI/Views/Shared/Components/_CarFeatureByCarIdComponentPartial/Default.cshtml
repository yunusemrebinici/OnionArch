@model List<GetCarFeatureByIdDto>
@{

	int x = 0;

}
<div class="tab-pane fade show active" id="pills-description" role="tabpanel" aria-labelledby="pills-description-tab">


	

	<div class="row">

		<div class="col-md-4">
			<ul class="features">
				@foreach (var item in Model)
				{

					int stop = 3;
					if (Model[x].available == true)
					{
							<li class="check"><span class="ion-ios-checkmark"></span>@Model[x].featureName</li>
					}
					else
					{
						<li class="remove"><span class="ion-ios-close"></span>@Model[x].featureName</li>
					}
				
					if (x != stop) { x++; }
					else
					{
						break;
					}
				}

			</ul>
		</div>

		@{
			x++;
		}
		<div class="col-md-4">
			<ul class="features">
				@foreach (var item in Model)
				{

					int stop = 7;
					if (Model[x].available == true)
					{
						<li class="check"><span class="ion-ios-checkmark"></span>@Model[x].featureName</li>
					}
					else
					{
						<li class="remove"><span class="ion-ios-close"></span>@Model[x].featureName</li>
					}
					if (x != stop) { x++; }
					else
					{
						break;
					}
				}

			</ul>
		</div>
		@{
			x++;
		}
		<div class="col-md-4">
			<ul class="features">
				@foreach (var item in Model)
				{

					int stop = 11;
					if (Model[x].available == true)
					{
						<li class="check"><span class="ion-ios-checkmark"></span>@Model[x].featureName</li>
					}
					else
					{
						<li class="remove"><span class="ion-ios-close"></span>@Model[x].featureName</li>
					}
					if (x != stop) { x++; }
					else
					{
						break;
					}
				}

			</ul>
		</div>
	</div>
</div>
